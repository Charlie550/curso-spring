
spring:
  datasource:
    url: jdbc:sqlserver://localhost:61006;databaseName=HelpDesk
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: sa
    password: live550e60
  jpa:
    properties:
      hibernate:
        ddl-auto: update
        dialect: org.hibernate.dialect.SQLServer2012Dialect
        dll:
          auto: update
        format:
          sql: true
      show:
        sql: true


logging:
  level:
    root: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

